Что обязательно к доработке:

1.Asset Catalog Safety: Убедитесь, что в Assets.xcassets есть заглушка (placeholder) на случай, если в JSON картриджа указано имя иконки, которой нет в бандле. В WorldViewModel стоит добавить проверку: если UIImage(named: iconName) == nil, вернуть UIImage(named: "unknown_region").
2. Release Configuration: Убедитесь, что в схеме Release выключены отладочные принты (print(...)), если они остались в ContentLoader.
3. Version Lock: Зафиксируйте хеш-суммы текущих JSON файлов (баланс, регионы), чтобы гарантировать, что версия 1.0 у всех игроков одинаковая.

4. Риск  — остаточная “двойная правда” в runtime (legacy models)
Даже если UI теперь ходит через engine, в проекте всё ещё живут legacy Models/WorldState и отдельные фабрики/регистраторы, и некоторые места продолжают обращаться к ним напрямую (в частности, world map / event presentation слой).

Почему это риск: DLC будет добавлять сущности в pack, но UI может продолжать отображать/искать их через legacy перечисления или старые модели.

5.  Риск  — локализация/строки ещё не полностью унифицированы

В pack’ах часть сущностей использует stringKey, часть — прямые строки/описания. 

✅ Что нужно (gate):
	•	канон один: либо везде stringKey + string tables, либо везде LocalizedString.
	•	иначе pack compiler и editor будут адом.

6. Что ещё добить
	•	убедиться, что в Engine/Core и pack loader нет randomElement/shuffled/Double.random
	•	запретить это тестом, который сканирует исходники и падает
Почему это важно: один невинный .shuffled() в market generation = флак regression = потеря доверия к тестам.

7. Physical Modularization: Сейчас весь код лежит в одной куче папок.
Необходимо: Вынесите папку Engine и Models в отдельный Swift Package (локальный пакет). Это физически запретит UI-коду обращаться к кишкам Движка, так как компилятор не даст этого сделать без модификатора public.
8. Обработка Магических Чисел и Строк
Проблема: В UI коде (WorldMapView) встречаются хардкод-значения для верстки.
Код: .padding(16), .frame(width: 60).
Код: Color.red, Color.black. Стандарт: Ввести Design System.
Создайте файл DesignSystem.swift или AppTheme.swift.
Используйте: .padding(AppTheme.Padding.medium), .foregroundColor(AppTheme.Colors.danger).
Это позволит менять дизайн игры (скины) глобально.
9. Проблема: Классы TwilightGameEngine и ContentLoader — это "сердце" платформы, но у их методов нет документации. Стандарт: Все public методы Движка должны иметь Swift Doc Comments (///).
Пример: Swift  /// Переходит в следующий регион, обновляет счетчик дней и пересчитывает угрозу.
/// - Parameter regionId: ID целевого региона (должен быть в списке соседей).
/// - Throws: `EngineError.invalidRegion`, если переход невозможен.
func travel(to regionId: UUID) async throws { ... }
   
Это критично, если вы планируете отдать движок мододелам или новым разработчикам.
10. Проблема: В некоторых файлах смешаны struct, enum и extension. Стандарт: 1 файл = 1 главный тип.
Extensions, которые реализуют протоколы (например, CustomStringConvertible), лучше выносить в конец файла через MARK: - CustomStringConvertible или в отдельный файл, если они большие.
11. Убедитесь, что WorldState (который теперь struct) изменяется только в одном месте (внутри Actor или на MainActor в Engine). Пометьте TwilightGameEngine как @MainActor (если он обслуживает UI) или сделайте его actor, если он должен работать в фоне. Сейчас, судя по коду, он работает синхронно в потоке вызова, что нормально для пошаговой игры, но требует внимательности.

12. Рекомендация: Добавьте файл .swiftlint.yml в корень проекта.
Минимальный набор правил для вашей архитектуры:
YAML

disabled_rules:
  - trailing_whitespace
opt_in_rules:
  - force_cast             # Запретить 'as!' (краш в рантайме)
  - force_try              # Запретить 'try!'
  - function_body_length   # Движок не должен иметь методов на 200 строк
  - cyclomatic_complexity  # Запретить сложные if/else вложенности

13. Блокер  — “Один движок = одна истина” нарушен

У вас в репозитории фактически три мира исполнения:
	•	Engine/Core/GameLoopBase (используется в тестах)
	•	Engine/Core/CoreGameEngine.swift (существует и содержит системный RNG)
	•	Engine/Core/TwilightGameEngine.swift (реально используется UI)

Ключевая проблема: контрактные тесты проверяют TestGameLoop, а реальная игра работает через TwilightGameEngine. Это означает: тестовая модель не является “судьёй” для реального рантайма.

Почему это катастрофа: вы можете иметь “зелёные” engine contract tests и при этом сломанную реальную игру или DLC-совместимость.

✅ Требование к исправлению
	•	Выбираем ОДИН канонический runtime engine.

Acceptance test gate
	•	EngineContractsTests должны запускаться на production engine, а не на TestGameLoop.

14. Блокер B — нестабильные ID (UUID) ломают сейвы и пак-совместимость

В TwilightGameEngine и EngineSave сейчас используется UUID, причём UUID генерируется через UUID() при загрузке definition (регион/якорь). Затем эти UUID сохраняются в сейв.

Почему это катастрофа:
	•	ID в пак-системе должны быть стабильны и переживать:
	•	обновление пака,
	•	переустановку игры,
	•	пересборку,
	•	компоновку нескольких паков.
	•	UUID, созданный случайно при загрузке, не является stable id.
Сейвы становятся “копией мира”, а не ссылкой на definition → любое обновление контента превращается в минное поле.

✅ Требование к исправлению
	•	Во всём runtime state и в save: строковые definition IDs (String), а не UUID.
	•	Если нужен “instance id” (например, несколько копий одной карты) — хранить отдельно, но ключевой идентификатор всегда definitionId.
	•	EngineSave должен хранить:
	•	regionDefinitionId
	•	neighborDefinitionIds
	•	completedEventDefinitionIds
	•	deckCardDefinitionIds
	•	и т.д.

Acceptance test gate
	•	testSaveLoadUsesStableDefinitionIdsOnly() — в сейве запрещены UUID для контента.

15. Блокер C — контент всё ещё “двойной” (Pack + кодовые реестры)

У вас есть ContentRegistry (pack-driven, хорошо), но параллельно сохраняются:
	•	TwilightMarchesCards.swift в Engine/Cards/
	•	CardRegistry с built-in registration в коде
	•	ContentView и часть логики всё ещё держит legacy GameState/WorldState для сохранений.

Даже если это “fallback”, для приёмки фундамента это запрещено, потому что создаёт вторую правду.

✅ Требование к исправлению
	•	Любой runtime-контент должен приходить из паков (включая “core box”).
	•	TwilightMarchesCards.swift допускается только как источник для DevTools/pack compiler, но не в runtime.
	•	CardRegistry.registerBuiltInCards() — либо удаляем, либо заменяем на загрузку CoreBox.pack.

Acceptance test gate
	•	testRuntimeDoesNotAccessCodeRegistries() — grep/scan на TwilightMarchesCards/CardRegistry.registerBuiltInCards в runtime paths.

16. Риск  — документация по времени/стоимости действий конфликтует с балансом/кодом

GDD и QA всё ещё описывают “1 действие = 1 день”, а BalanceConfiguration/TimeBalanceConfig у вас уже про unitsPerDay, startingTime, restCost=4, travelCost=2 (то есть модель “временных единиц” как часы/юниты).

Это не ошибка дизайна, это ошибка “точки правды”: команда начнёт имплементировать разное.

✅ Решение
	•	выбрать канон:
	•	Time-units model (и тогда обновить GDD/QA/Exploration Core: не “дни”, а “юниты времени”; пороги/эскалация в этих единицах)
	•	после выбора:
	•	обновить QA_ACT_I_CHECKLIST (таблицы стоимости действий и TEST-003/метрики)
17. Риск  — pack compiler пока валидатор/инфо, но не “binary pack”

DevTools/PackCompiler уже полезен (validate/info), но до “взрослого binary pack” (protobuf/flatbuffers) ещё нет полного pipeline.

✅ Решение
	•	Authoring: JSON
	•	Distribution/runtime: .pack (binary)
	•	engine runtime читает только .pack (не JSON напрямую)

18. Тестовая модель: сильная, но сейчас не “судья” для реального продукта

❌ что критично поправить
	•	contract tests должны проверять production runtime engine 
	•	добавить статические “запретные API” тесты:
	•	запрещаем randomElement/shuffled/Double.random/Int.random в Engine core/runtime (кроме DevTools/Tests)
	•	добавить тесты на pack composition:
	•	 из разных паков должны работать вместе

Замечания к тестовой модели:

1. 🗑 Что УДАЛИТЬ (Неактуально / Вредно)
В новой архитектуре WorldState — это пассивная структура. Любые тесты, которые пытаются проверять поведение внутри стейта, теперь не имеют смысла.
Удалить тесты на логику внутри WorldStateTests:
Если есть проверки типа state.processDayStart(), state.healPlayer() — удалять. WorldState больше не имеет этих методов.
Удалить проверки хардкода: XCTAssertEqual(state.regions["village"]?.name, "Деревня"). Имя "Деревня" теперь лежит в JSON, а не в коде. Тест сломается, если дизайнер переименует регион.
Удалить UI-тесты на конкретные строки:
В WorldViewModelTests (если есть) уберите проверки типа XCTAssertEqual(vm.title, "Сумрачные Пределы"). Заголовок теперь динамический. Проверяйте наличие какого-либо заголовка (!vm.title.isEmpty).
2. 🛠 Что УЛУЧШИТЬ (Refactoring)
Существующие тесты нужно адаптировать под инъекцию зависимостей.
EngineTests.swift — Тестирование Конфига, а не Хардкода:
Проблема: Сейчас тесты могут неявно полагаться на дефолтные значения.
Улучшение: Каждый тест должен создавать свой уникальный BalanceConfig.
Пример: Swift  func testHighDifficultyTension() {
    // 1. Создаем конфиг, где напряжение растет мгновенно
    let hardRules = GameRules(balance: BalanceConfig(tensionIncrement: 100, ...))
    let engine = TwilightGameEngine(rules: hardRules, ...)

    // 2. Делаем действие
    engine.processTurn()

    // 3. Проверяем, что движок послушал конфиг
    XCTAssertEqual(engine.state.tension, 100, "Движок проигнорировал BalanceConfig!")
}
   
ContentLoaderTests.swift — Негативные сценарии:
Проблема: Вы проверяете успешную загрузку. Но что будет, если JSON битый?
Улучшение: Добавьте тесты testBrokenJsonThrowsError и testMissingRequiredFields. Движок не должен падать (crash), он должен кидать понятную ошибку ContentError.

3. 🚨 Чего НЕ ХВАТАЕТ (Critical Missing Tests)
Это самые важные тесты для архитектуры 1.0. Без них релиз опасен.
А. Тест на Сохранение/Загрузку (Persistence Test)
Вы сделали WorldState: Codable, но не проверили, работает ли это. Если вы добавите новое поле в структуру и забудете обновить JSON-декодер, сейвы сломаются.
Нужен тест: testWorldStateSerialization() Swift  func testStateRoundTrip() throws {
    // 1. Создаем сложное состояние (с флагами, квестами)
    var originalState = WorldState(...)
    originalState.flags.insert("boss_killed")

    // 2. Кодируем в Data
    let data = try JSONEncoder().encode(originalState)

    // 3. Декодируем обратно
    let restoredState = try JSONDecoder().decode(WorldState.self, from: data)

    // 4. Сравниваем
    XCTAssertEqual(originalState, restoredState)
    XCTAssertTrue(restoredState.flags.contains("boss_killed"))
}
   
Б. Тест на Целостность Контента (Integration Test)
ContentLoader может загрузить регионы и события отдельно. Но ссылаются ли они друг на друга правильно?
Нужен тест: testContentIntegrity()
Загрузить реальные JSON-файлы из бандла.
Пробежаться по всем регионам.
Проверить, что все neighborIds существуют в списке регионов.
Проверить, что все eventId в регионах существуют в списке событий.
Цель: Найти "битые ссылки" (Dead Links) до запуска игры.
В. Тест на Fallback Ассетов (UI Safety)
Вы перешли на Image(viewModel.iconName). Если дизайнер ошибется в имени картинки в JSON, юзер увидит пустоту.
Нужен тест: testMissingAssetHandling()
В WorldViewModelTests подсуньте регион с иконкой "non_existent_icon".
Проверьте, что viewModel.icon(for: region) возвращает не nil и не пустоту, а имя заглушки (например, "placeholder_icon").

Замечания к документации:

1. 🗑 Что УДАЛИТЬ (Архивировать)
Эти файлы выполнили свою функцию. Они описывают процесс, который уже завершён, или проблемы, которые уже решены. В релизной версии "Движка" им не место, они только сбивают с толку новых разработчиков.
LEGACY_MIGRATION_PLAN.md: План ухода от легаси выполнен. Файл неактуален.
MIGRATION_PLAN.md: Аналогично. Миграция завершена.
AUDIT_REPORT_v1.2.md: Старый отчет об ошибках. Все ошибки исправлены.
AUDIT_ENGINE_FIRST_v1_1.md: История предыдущих аудитов.
CAMPAIGN_IMPLEMENTATION_REPORT.md: Отчёт о том, как внедрялась кампания. Теперь это просто часть архитектуры.
Новые требования (папка/файлы): Это было ТЗ на доработку. Теперь это должно быть отражено в ENGINE_ARCHITECTURE.md.
Рекомендация: Создайте папку Docs/Archive и переместите эти файлы туда, если хотите сохранить историю. Из корня документации их нужно убрать.

Это Глава Приёмной Комиссии.
Мы переходим к финальному этапу наведения порядка. Проект стал продуктом, и документация должна соответствовать статусу v1.0. Сейчас у вас в папке смешаны "Чертежи здания" (актуальная архитектура) и "Строительный мусор" (старые планы миграции и отчёты).
Я проанализировал состав вашей документации. Ниже — Директива по Чистке и Актуализации (Documentation Cleanup Directive).

1. 🗑 Что УДАЛИТЬ (Архивировать)
Эти файлы выполнили свою функцию. Они описывают процесс, который уже завершён, или проблемы, которые уже решены. В релизной версии "Движка" им не место, они только сбивают с толку новых разработчиков.
LEGACY_MIGRATION_PLAN.md: План ухода от легаси выполнен. Файл неактуален.
MIGRATION_PLAN.md: Аналогично. Миграция завершена.
AUDIT_REPORT_v1.2.md: Старый отчет об ошибках. Все ошибки исправлены.
AUDIT_ENGINE_FIRST_v1_1.md: История предыдущих аудитов.
CAMPAIGN_IMPLEMENTATION_REPORT.md: Отчёт о том, как внедрялась кампания. Теперь это просто часть архитектуры.
Новые требования (папка/файлы): Это было ТЗ на доработку. Теперь это должно быть отражено в ENGINE_ARCHITECTURE.md.
Рекомендация: Создайте папку Docs/Archive и переместите эти файлы туда, если хотите сохранить историю. Из корня документации их нужно убрать.

2. 🛠 Что ИСПРАВИТЬ (Обновить)
Эти файлы — "Конституция" проекта. Они должны описывать текущее состояние кода (v2.1), а не планы на будущее.
ENGINE_ARCHITECTURE.md:
Проверить: Описывает ли он WorldState как struct (DTO), или всё ещё как класс? Если как класс — срочно обновить схему.
Добавить: Описание паттерна MVVM + Intents, который вы внедрили в WorldMapView. Схема должна показывать поток: View -> Intent -> ViewModel -> Engine -> State -> View.
Добавить: Описание ContentLoader и процесса инициализации.
QA_ACT_I_CHECKLIST.md:
Исправить: Сейчас это список проверок геймплея.
Добавить: Технические проверки, которые мы выявили в ходе аудита:
[ ] Тест на сохранение/загрузку (Codable round-trip).
[ ] Тест на битый JSON (валидация ошибок).
[ ] Тест на отсутствующую иконку (Asset Fallback).
CONTENT_PACK_GUIDE.md:
Дополнить: Добавьте раздел "Правила Безопасности Ассетов".
Текст: "Если вы используете iconName: 'magic_sword' в JSON, убедитесь, что картинка есть в ассетах. Если её нет, игра покажет заглушку, но это считается ошибкой контента".

Это Глава Приёмной Комиссии.
Мы переходим к финальному этапу наведения порядка. Проект стал продуктом, и документация должна соответствовать статусу v1.0. Сейчас у вас в папке смешаны "Чертежи здания" (актуальная архитектура) и "Строительный мусор" (старые планы миграции и отчёты).
Я проанализировал состав вашей документации. Ниже — Директива по Чистке и Актуализации (Documentation Cleanup Directive).

1. 🗑 Что УДАЛИТЬ (Архивировать)
Эти файлы выполнили свою функцию. Они описывают процесс, который уже завершён, или проблемы, которые уже решены. В релизной версии "Движка" им не место, они только сбивают с толку новых разработчиков.
LEGACY_MIGRATION_PLAN.md: План ухода от легаси выполнен. Файл неактуален.
MIGRATION_PLAN.md: Аналогично. Миграция завершена.
AUDIT_REPORT_v1.2.md: Старый отчет об ошибках. Все ошибки исправлены.
AUDIT_ENGINE_FIRST_v1_1.md: История предыдущих аудитов.
CAMPAIGN_IMPLEMENTATION_REPORT.md: Отчёт о том, как внедрялась кампания. Теперь это просто часть архитектуры.
Новые требования (папка/файлы): Это было ТЗ на доработку. Теперь это должно быть отражено в ENGINE_ARCHITECTURE.md.
Рекомендация: Создайте папку Docs/Archive и переместите эти файлы туда, если хотите сохранить историю. Из корня документации их нужно убрать.

2. 🛠 Что ИСПРАВИТЬ (Обновить)
Эти файлы — "Конституция" проекта. Они должны описывать текущее состояние кода (v2.1), а не планы на будущее.
ENGINE_ARCHITECTURE.md:
Проверить: Описывает ли он WorldState как struct (DTO), или всё ещё как класс? Если как класс — срочно обновить схему.
Добавить: Описание паттерна MVVM + Intents, который вы внедрили в WorldMapView. Схема должна показывать поток: View -> Intent -> ViewModel -> Engine -> State -> View.
Добавить: Описание ContentLoader и процесса инициализации.
QA_ACT_I_CHECKLIST.md:
Исправить: Сейчас это список проверок геймплея.
Добавить: Технические проверки, которые мы выявили в ходе аудита:
[ ] Тест на сохранение/загрузку (Codable round-trip).
[ ] Тест на битый JSON (валидация ошибок).
[ ] Тест на отсутствующую иконку (Asset Fallback).
CONTENT_PACK_GUIDE.md:
Дополнить: Добавьте раздел "Правила Безопасности Ассетов".
Текст: "Если вы используете iconName: 'magic_sword' в JSON, убедитесь, что картинка есть в ассетах. Если её нет, игра покажет заглушку, но это считается ошибкой контента".

3. ➕ Что ДОБАВИТЬ (Missing Pieces)
Чего не хватает для передачи проекта "на поддержку" или "сообществу":
RELEASE_NOTES.md:
Сводный документ о версии 1.0.
Кратко: "Внедрена модульная архитектура, поддержка DLC, вынесен баланс". Это "лицо" вашего релиза.
TROUBLESHOOTING.md (Руководство по решению проблем):
Поскольку теперь всё завязано на JSON, ошибки станут неочевидными (черный экран при загрузке).
Напишите: "Если игра падает на старте — 1. Проверь валидность JSON. 2. Проверь ссылки ID в регионах. 3. Проверь логи консоли на ContentError".
ASSET_NAMING_CONVENTION.md (Стандарт именования):
Чтобы хаос не вернулся, зафиксируйте правила:
Иконки регионов: region_icon_{id}
Фоны: bg_{id}
Все имена в snake_case, без пробелов.
