# 🛠 Инструменты сборки контекста (DevTools)

Эта папка содержит скрипты для автоматической выгрузки проекта в текстовые дампы. Это необходимо для передачи контекста ИИ (ChatGPT/Claude) при аудите архитектуры и код-ревью.

## 🚀 Как собрать данные (Инструкция)

1. Откройте **Терминал**.
2. Перейдите в **корневую папку проекта** (туда, где лежит `.xcodeproj`):
   ```bash
   cd "/Users/abondarenko/Library/Mobile Documents/com~apple~CloudDocs/XCode/CardSampleGame"
   ```
   *(Совет: Если путь изменился, просто напишите `cd ` и перетащите папку проекта из Finder в окно терминала)*

3. Запустите скрипт сборки:
   ```bash
   python3 DevTools/collect_all.py
   ```

## 📂 Результат (Output)

Скрипт создаст папку **`DevTools/Output/`** и положит туда 4 файла. 
**Важно передавать их все**, так как архитектура распределена между кодом и JSON-данными.

| Файл | Содержимое | Зачем нужен |
| :--- | :--- | :--- |
| **1. PROJECT_CODE_DUMP.txt** | Весь код `.swift` (Engine, UI, ViewModels). Без тестов. | Анализ логики, архитектуры, UI. |
| **2. DATA_DUMP.txt** | Все `.json` файлы (ContentPacks, Data, Balance). | **Критически важен!** Тут лежат баланс, регионы, враги и конфиги картриджей. |
| **3. TESTS_DUMP.txt** | Код из папки `.Tests`. | Проверка покрытия тестами и качества QA. |
| **4. DOCS_DUMP.txt** | Все `.md` файлы (Спецификации, Планы, Changelog). | Понимание требований и истории изменений. |

## ⚠️ Частые ошибки

* **Error: No such file or directory:** Вы находитесь не в корне проекта. Выполните команду `cd` (шаг 2).
* **Скрипт не видит новые папки:** Если вы добавили новую папку в корень (например, `NewFeatures`), она попадет в дампы автоматически. Если нужно исключить — правьте переменную `IGNORE_DIRS_SYSTEM` внутри `collect_all.py`.

---
*Generated for CardSampleGame v1.0 (Engine + Cartridge Architecture)*
