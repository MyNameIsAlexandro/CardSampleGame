# 🛠 Инструменты для сборки проекта (DevTools)

Эта папка содержит скрипты для автоматической сборки кода, тестов и документации в текстовые дампы для анализа ИИ (Claude/ChatGPT).

## 🚀 Как запустить сборку

1. Откройте **Терминал**.
2. Перейдите в **корневую папку проекта**:
   ```bash
   cd "/Users/abondarenko/Library/Mobile Documents/com~apple~CloudDocs/XCode/CardSampleGame"
   ```

3. Запустите скрипт:
   ```bash
   python3 DevTools/collect_all.py
   ```

## 📂 Результат

После успешного выполнения в папке **`DevTools/Output/`** появятся три файла:

1. **`PROJECT_CODE_DUMP.txt`** — Весь Swift-код проекта (без тестов).
2. **`TESTS_DUMP.txt`** — Все файлы из папки тестов.
3. **`DOCS_DUMP.txt`** — Вся документация + важные файлы из корня (`AUDIT...`, `CHANGELOG...`).

Эти файлы готовы к отправке.
